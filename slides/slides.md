# Deprecated monolithic deck

Slide decks are built per file under `slides/pages/` via `npm run slidev:build` (see `scripts/build-slidev-decks.mjs`). Order is defined in `slides/deck-order.txt`. Shared Slidev head frontmatter lives in `slides/deck-entry-header.in.yaml`.
