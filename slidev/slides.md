# Deprecated monolithic deck

Slide decks are built per file under `slidev/pages/` via `npm run slidev:build` (see `scripts/build-slidev-decks.mjs`). Order is defined in `slidev/deck-order.txt`. Shared Slidev head frontmatter lives in `slidev/deck-entry-header.in.yaml`.
