---
slug: draft-readme
description: 说明 docs/draft 目录用途；不参与站点发布。
---

# 草稿目录

此目录存放**尚未设置合法 `date`（`YYYY-MM-DD`）**的 Markdown，用于本地整理与写作。

站点构建时不会把 `draft/` 下的文件当作正式文章（不出现在首页、文章路由与 RSS）。发布前请将文件移回 `docs/` 下合适位置，并补齐 frontmatter 中的 `date` 等必填字段。
